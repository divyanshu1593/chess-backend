export enum ErrorMessages {
  ALLOWED_ORIGIN_NOT_CONFIGURED = `"ALLOWED_ORIGIN" must be configured in env for production environment`,
  ORIGIN_NOT_ALLOWED = `Origin not allowed`,
  SUBSCRIBER_LIMIT_REACHED = 'Subscriber limit reached, cannot subscribe anymore',
}
